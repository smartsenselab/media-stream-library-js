import { Tube } from '../component';
/**
 * A component that converts raw binary data into Elementary packets on
 * the incoming stream.
 * @extends {Component}
 */
export declare class ElementaryParser extends Tube {
    /**
     * Create a new Elementary parser component.
     * @return {undefined}
     */
    constructor();
}
